El último corolario del inciso anterior permite definir la **dimensión** de un espacio vectorial de dimensión finita como el **número de elementos de una base cualquiera de** $V$.

**Definición**. Sea $V$ espacio vectorial de dimensión finita. Diremos que $n$ es la *dimensión* de $V$ y denotaremos $\dim V = n$, si existe una base de $V$ de $n$ vectores. 
Si $V = \{0\}$, entonces definimos $\dim V = 0$.

Como ya demostramos anteriormente, si $V$ es un espacio vectorial de dimensión finita y $\mathcal{B}, \mathcal{B}'$ dos bases de $V$, entonces $|\mathcal{B}| = |\mathcal{B}'|$.  
****
### **Más de $n$ vectores sobran (son LD), menos de $n$ vectores faltan (no generan)**
**Corolario.** Sea $V$ un espacio vectorial de dimensión finita y sea $n = \dim V$. Entonces

1. Cualquier subconjunto de $V$ que contenga más de $n$ vectores es linealmente dependiente;
2. Ningún subconjunto de $V$ con menos de $n$ vectores puede generar $V$.

**Demostración**.
($1$) Sea $\{v_1, \dots, v_n\}$ una base de $V$, entonces $v_1, \dots, v_n$ generan $V$, luego, ya que **[[D-Un espacio construido con x vectores no puede contener más de x direcciones independientes]]**, cualquier subconjunto de $V$ que contenga más de $n$ vectores es LD.

($2$) Sea $S$ subconjunto de $V$ con $m < n$ vectores. Si $S$ genera $V$, entonces todo subconjunto de más de $m$ vectores es LD (teorema anterior y citado en ($1$)), por lo tanto, un subconjunto de $n$ vectores es LD. En consecuencia, no puede haber una base de $n$ elementos, lo cual contradice la hipótesis. $\square$
****
#### **[[D-En una base, cada vector tiene una receta única e irrepetible]]**
**Proposición.** Sea $V$ un espacio vectorial, y sea $\mathcal{B} = \{w_1, \dots, w_n\}$ una base (finita) de $V$. Entonces para todo $v \in V$ existen únicos escalares $a_1, \dots, a_n$ tales que
$$
v = a_1w_1 + \cdots + a_nw_n \quad \text{(de manera única).}
$$
****
#### **Definición (Notación).** Si $\mathcal{B} = \{v_1, \dots, v_n\}$ es una base ordenada de $V$...
y $v \in V$ se escribe (de manera única) por $v = \sum a_i v_i$, entonces
$$
[v]_\mathcal{B} = (a_1, \dots, a_n) \in K^n
$$
son las **coordenadas** de $v$ respecto de la base $\mathcal{B}$.
****
#### **[[D-Añadir un vector externo al generador de un conjunto L.I. preserva su independencia lineal]]**
    
**Lema.** Sea $S$ un subconjunto L.I. de un espacio vectorial $V$. Supongamos que $w \in V$ no pertenece al subespacio generado por $S$. Entonces $S \cup \{w\}$ es L.I.
****
#### **[[D-Todo conjunto linealmente independiente se puede extender hasta formar una base]]**
**Corolario**. Sea $W$ un subespacio de un espacio vectorial $V$ con dimensión finita $n$ y $S_0$ un subconjunto LI de $W$. Entonces, $S_0$ se puede completar a una base de $W$.
****
#### **[[D-Subespacio propio, dimensión menor]]**
**Corolario**. Si $W$ es un subespacio propio de un espacio vectorial de dimensión finita $V$, entonces $W$ es de dimensión finita y $\dim W < \dim V$.

(Cabe aclarar que cuando usamos el término "propio" para describir un subespacio, nos referimos a que un **subespacio propio** $W$ de un espacio vectorial $V$ es aquel que está contenido dentro de $V$, pero que **es estrictamente más pequeño que $V$**).
****
**Corolario.** Sea $W \subseteq V$ un subespacio vectorial de $V$ con $\dim V < \infty$.
Si $\dim W = \dim V$ entonces $W = V$.
****
**Corolario.** Si $\dim V = n$ y $\mathcal{B} = \{v_1, \dots, v_n\}$ es L.I., entonces $\mathcal{B}$ es una base de $V$.

**Demostración.** Si $\mathcal{B}$ no genera $V$, entonces $\exists v \in V$ tal que $v \notin \langle \mathcal{B} \rangle$, luego $\widetilde{\mathcal{B}} = \mathcal{B} \cup \{v\}$ es L.I. y tiene $n + 1$ elementos, pero $V$ puede ser generado por un conjunto de $n$ elementos (pues $\dim V = n$). Absurdo. $\square$
****
#### **[[D-Toda matriz cuadrada con filas linealmente independientes tiene inversa]]**
**Corolario**. Sea $A$ una matriz $n \times n$ sobre el cuerpo $K$ y supongamos que los vectores fila de $A$ forman un conjunto L.I. de vectores de $K^n$. Entonces $A$ es invertible.
****
#### **[[D-Todo conjunto de generadores de un espacio de dimensión finita contiene un subconjunto que es base]]**
**Proposición.** Sea $V$ un espacio vectorial de dimensión finita. Sea $S = \{v_1, \dots, v_m\}$ un conjunto de generadores de $V$. Entonces existe un subconjunto $\mathcal{B} \subseteq S$ que es una base de $V$.

Se adjunta ejemplo...
**[[E-Todo conjunto de generadores de un espacio de dimensión finita contiene un subconjunto que es base]]**
****
#### **[[D-Las columnas de una matriz cuadrada invertible forman necesariamente una base del espacio $K^n$]]**
(*espacio $K^n$*)
**Proposición.** Si $P$ es una matriz invertible $n \times n$ (con coeficientes en $K$), entonces las columnas de $P$ forman una base de $K^n$.
****
#### **[[D-Dimensión de la suma, suma las partes, resta la intersección]]**
**Teorema**. Si $W_1$ y $W_2$ son subespacios de dimensión finita de un espacio vectorial, entonces $W_1 + W_2$ es de dimensión finita y
$$
\dim W_1 + \dim W_2 = \dim(W_1 \cap W_2) + \dim(W_1 + W_2).
$$